# Event highlights

Place media here for the **Schedule** page “Event highlights” section.

- **group-photo.jpg** — One group photo (from your `Pictures` folder). Update `content/events-media.ts` if you use a different filename.
- For performance videos, place files in `public/videos/performances/` and set their paths in `content/events-media.ts`.
