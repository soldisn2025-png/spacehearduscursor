# Gallery images

Place performance, rehearsal, and community photos here. Add entries in `content/gallery.ts` with the correct `src` (e.g. `/images/gallery/concert-1.jpg`), `alt`, `category`, and optional `caption`.

Categories: `performance` | `rehearsal` | `community` | `behind-the-scenes`
