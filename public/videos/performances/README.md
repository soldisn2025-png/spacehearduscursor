# Performance videos

Place video files here (e.g. .mp4). Reference them in `content/videos.ts` by path, e.g. `/videos/performances/featured.mp4`.

- **featured.mp4** — Optional: main featured video on the homepage.
- Add more files and list them in `content/videos.ts`.
